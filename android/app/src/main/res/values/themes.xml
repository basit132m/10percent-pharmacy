<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the UI; this only styles the window before it loads. -->
    <style name="Theme.PharmacyBonus" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/brand_green</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>
